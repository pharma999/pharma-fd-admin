import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ProviderApp smoke test', (WidgetTester tester) async {
    // Basic smoke test — app entry point is SplashScreen.
    // Full integration tests require a running backend.
    expect(true, isTrue);
  });
}
