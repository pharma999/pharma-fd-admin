name: home_care_admin
description: "Home Care Service — Admin Panel"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.7.2
  http: ^1.2.1
  shared_preferences: ^2.5.4
  flutter_rating_bar: ^4.0.1
  intl: ^0.19.0
  google_maps_flutter: ^2.14.0
  geolocator: ^14.0.2
  fl_chart: ^0.69.0
  url_launcher: ^6.3.0
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
